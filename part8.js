console.log('Install external package in Node.js');

const colors = require('colors'); // install colors package using npm i colors
console.log(colors.red("Stop"));
console.log(colors.yellow("Stop, wait for green signal"));
console.log(colors.green("We can go...!"));
console.log(colors.rainbow("Create own package.json"));

// create own package.json file using npm init.